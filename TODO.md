- Stop creating the database with every function launch.
- 
2. Непоследовательные коды ошибок
Длинный пароль → HTTP 500 (вместо 400)
Дублирующий email → HTTP 500 (вместо 409)
SQL инъекция через пароль → HTTP 500 с сообщением "email already exists"
3. Уязвимость к SQL инъекциям
Тесты с SQL инъекциями через email и имя проходят успешно (создаются пользователи), что указывает на потенциальную уязвимость.