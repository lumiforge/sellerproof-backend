meta {
  name: Get audit logs with invalid JWT token
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/admin/audit-logs
  auth: bearer
}

auth:bearer {
  token: "invalid.jwt.token"
}

tests {
  test('Should return 401 Unauthorized for invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}
