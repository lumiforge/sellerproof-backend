meta {
  name: Get audit logs without JWT token
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/admin/audit-logs
  auth: none
}

tests {
  test('Should return 401 Unauthorized when JWT token is missing', function() {
    expect(res.status).to.equal(401);
  });
}
