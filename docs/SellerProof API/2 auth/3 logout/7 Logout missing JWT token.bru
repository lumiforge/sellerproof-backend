meta {
  name: Logout missing JWT token
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: json
}

headers {
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}

tests {
  test('Should return 401 for missing JWT token', function() {
    expect(res.status).to.equal(401);
  });
}