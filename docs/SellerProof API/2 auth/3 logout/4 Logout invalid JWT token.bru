meta {
  name: Logout invalid JWT token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: json
}

headers {
  Authorization: Bearer invalid.jwt.token
}

body:json {
  {
    "refresh_token": "{{adminRefreshToken}}"
  }
}

tests {
  test('Should return 401 for invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}