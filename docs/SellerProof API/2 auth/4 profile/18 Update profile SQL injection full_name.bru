meta {
  name: Update profile SQL injection full_name
  type: http
  seq: 18
}

put {
  url: {{baseUrl}}/api/v1/auth/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "full_name": "'; DROP TABLE users; --"
  }
}

tests {
  test('Should return 400 for SQL injection in full_name', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Bad Request');
  });
}