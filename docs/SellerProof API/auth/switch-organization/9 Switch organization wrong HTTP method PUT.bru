meta {
  name: Switch organization wrong HTTP method PUT
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/api/v1/auth/switch-organization
  auth: inherit
}

tests {
  test('Should return 405 Method Not Allowed for PUT request to switch organization endpoint', function() {
    expect(res.status).to.equal(405);
  });
}