meta {
  name: Register XSS organization_name
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@example.com",
    "password": "password123",
    "full_name": "Test User",
    "organization_name": "<script>alert('XSS')</script>"
  }
}

tests {
  test('Should return 400 for XSS in organization_name', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Bad Request');
  });
}