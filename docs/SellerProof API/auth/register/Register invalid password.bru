meta {
  name: Register invalid password
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@example.com",
    "password": "123",
    "full_name": "Test User"
  }
}

tests {
  test('Should return 400 for invalid password length', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('password');
  });
}