meta {
  name: Refresh wrong Content-Type
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "valid_refresh_token_string"
  }
}

headers {
  Content-Type: text/plain
}

tests {
  test('Should return 415- Unsupported Media Type for wrong Content-Type', function() {
    expect(res.status).to.equal(415);
  });
}