meta {
  name: Refresh token null value
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": null
  }
}

tests {
  test('Should return 400 Bad Request for null refresh_token value', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Bad Request');
  });
}