meta {
  name: Refresh missing Content-Type
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "valid_refresh_token_string"
  }
}

headers {
  Content-Type:
}

tests {
  test('Should return 415- Unsupported Media Type for missing Content-Type header', function() {
    expect(res.status).to.equal(415);
  });
}