meta {
  name: Refresh wrong HTTP method GET
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "valid_refresh_token_string"
  }
}

tests {
  test('Should return 405 Method Not Allowed for GET request', function() {
    expect(res.status).to.equal(405);
  });
}