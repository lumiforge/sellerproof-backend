meta {
  name: Refresh SQL injection time
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9'; WAITFOR DELAY '00:00:05' --"
  }
}

tests {
  test('Should return 401 Unauthorized for SQL injection time-based attempt in refresh_token', function() {
    expect(res.status).to.equal(401);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Unauthorized');
  });
}