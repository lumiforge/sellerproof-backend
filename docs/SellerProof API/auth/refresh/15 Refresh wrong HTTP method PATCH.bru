meta {
  name: Refresh wrong HTTP method PATCH
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "valid_refresh_token_string"
  }
}

tests {
  test('Should return 405 Method Not Allowed for PATCH request', function() {
  });
}