meta {
  name: Get profile with malformed JWT token
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: "not.a.valid.jwt"
}

tests {
  test('Should return 401 Unauthorized for malformed JWT token', function() {
    expect(res.status).to.equal(401);
  });
}