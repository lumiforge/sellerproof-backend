meta {
  name: Health check POST
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/very-secret-health-check
  body: none
  auth: inherit
}

tests {
  test("Should return HTTP 405 Method Not Allowed", function() {
    expect(res.getStatus()).to.equal(405);
  });
}