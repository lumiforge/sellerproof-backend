meta {
  name: Health check PATCH
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/health
  body: none
  auth: inherit
}

tests {
  test("Should return HTTP 405 Method Not Allowed", function() {
    expect(res.getStatus()).to.equal(405);
  });
}