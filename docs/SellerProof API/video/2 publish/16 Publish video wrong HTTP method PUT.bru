meta {
  name: Publish video wrong HTTP method PUT
  type: http
  seq: 16
}

put {
  url: {{baseUrl}}/api/v1/video/publish
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 405 Method Not Allowed for PUT request', function() {
    expect(res.status).to.equal(405);
  });
}