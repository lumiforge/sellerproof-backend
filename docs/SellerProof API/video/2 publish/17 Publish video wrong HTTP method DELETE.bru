meta {
  name: Publish video wrong HTTP method DELETE
  type: http
  seq: 17
}

delete {
  url: {{baseUrl}}/api/v1/video/publish
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 405 Method Not Allowed for DELETE request', function() {
    expect(res.status).to.equal(405);
  });
}