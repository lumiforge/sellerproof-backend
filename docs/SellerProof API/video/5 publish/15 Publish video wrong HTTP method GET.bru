meta {
  name: Publish video wrong HTTP method GET
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/api/v1/video/publish
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 405 Method Not Allowed for GET request', function() {
    expect(res.status).to.equal(405);
  });
}