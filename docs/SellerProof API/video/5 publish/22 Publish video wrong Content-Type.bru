meta {
  name: Publish video wrong Content-Type
  type: http
  seq: 22
}

post {
  url: {{baseUrl}}/api/v1/video/publish
  body: text
  auth: bearer
}

headers {
  Content-Type: text/plain
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:text {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 415 Unsupported Media Type for wrong Content-Type', function() {
    expect(res.status).to.equal(415);
  });
}
