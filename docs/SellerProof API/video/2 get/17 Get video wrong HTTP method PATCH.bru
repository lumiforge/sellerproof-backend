meta {
  name: Get video wrong HTTP method PATCH
  type: http
  seq: 17
}

patch {
  url: {{baseUrl}}/api/v1/video?video_id={{testVideoId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PATCH request', function() {
    expect(res.status).to.be.at.least(405);
  });
}