meta {
  name: Get video wrong HTTP method POST
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/api/v1/video?video_id={{testVideoId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for POST request', function() {
    expect(res.status).to.be.at.least(405);
  });
}