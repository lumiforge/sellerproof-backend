meta {
  name: Complete upload with invalid JWT token
  type: http
  seq: 22
}

post {
  url: {{baseUrl}}/api/v1/video/upload/complete
  body: json
  auth: bearer
}

auth:bearer {
  token: invalid.jwt.token
}

body:json {
  {
    "video_id": "{{testVideoId}}",
    "parts": [
      {
        "part_number": 1,
        "etag": "d7a3decdb6280eb0ef3a059ac35ea0ee"
      },
      {
        "part_number": 2,
        "etag": "35eada417bf56dd1e6302107c1c23e4f"
      }
    ]
  }
}

tests {
  test("Should return 401 for invalid JWT token", function() {
    expect(res.status).to.equal(401);
  });
}