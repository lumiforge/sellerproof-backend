meta {
  name: Verify Manager is in Admin Org
  type: http
  seq: 32
}

get {
  url: {{baseUrl}}/api/v1/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{managerAccessToken}}
}


tests {
  test('Should be in admin org', function() {
    expect(res.status).to.equal(200);
    expect(res.body.org_id).to.equal(bru.getEnvVar("adminOrgId"));
  });
}