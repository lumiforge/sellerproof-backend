meta {
  name: Initiate upload wrong HTTP method
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/video/upload/initiate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "file_name": "test-video.mp4",
    "file_size_bytes": 1048576,
    "duration_seconds": 60
  }
}

tests {
  test('Should return 405 Method Not Allowed for wrong HTTP method', function() {
    expect(res.status).to.equal(405);
  });
}