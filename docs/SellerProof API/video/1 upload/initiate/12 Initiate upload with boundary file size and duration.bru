meta {
  name: Initiate upload with boundary file size and duration
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/v1/video/upload/initiate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "file_name": "test-video.mp4",
    "file_size_bytes": 0,
    "duration_seconds": 0
  }
}

tests {
  test('Should return 400 Bad Request for boundary file size and duration values', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Bad Request');
  });
}