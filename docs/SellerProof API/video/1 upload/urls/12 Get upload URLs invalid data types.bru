meta {
  name: Get upload URLs invalid data types
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/v1/video/upload/urls
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "video_id": 123,
    "total_parts": "5"
  }
}

tests {
  test("Should return 400 for invalid data types", function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property("error");
    expect(res.body).to.have.property("message");
  });
}