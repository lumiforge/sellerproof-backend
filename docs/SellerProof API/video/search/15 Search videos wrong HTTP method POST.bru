meta {
  name: Search videos wrong HTTP method POST
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/api/v1/video/search
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for POST method', function() {
    expect(res.status).to.equal(405);
  });
}