meta {
  name: Search videos wrong HTTP method PATCH
  type: http
  seq: 18
}

patch {
  url: {{baseUrl}}/api/v1/video/search
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PATCH method', function() {
    expect(res.status).to.equal(405);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Method not allowed');
  });
}