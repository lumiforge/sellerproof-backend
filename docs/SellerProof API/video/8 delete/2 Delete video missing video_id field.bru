meta {
  name: Delete video missing video_id (empty path param)
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/video/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 404 or 405 when video_id is missing in path', function() {
    // Go http.ServeMux вернет 404 или 405, если путь не совпадает с шаблоном {id}
    expect(res.status).to.be.oneOf([404, 405]);
  });
}
