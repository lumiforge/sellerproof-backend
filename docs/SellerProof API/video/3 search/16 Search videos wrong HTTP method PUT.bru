meta {
  name: Search videos wrong HTTP method PUT
  type: http
  seq: 16
}

put {
  url: {{baseUrl}}/api/v1/video/search
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PUT method', function() {
    expect(res.status).to.equal(405);
  });
}