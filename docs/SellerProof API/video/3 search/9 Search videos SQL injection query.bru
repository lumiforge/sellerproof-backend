meta {
  name: Search videos SQL injection query
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/video/search?query='; DROP TABLE videos; --
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 400 Bad Request for SQL injection in query parameter', function() {
    expect(res.status).to.equal(400);
  });
}