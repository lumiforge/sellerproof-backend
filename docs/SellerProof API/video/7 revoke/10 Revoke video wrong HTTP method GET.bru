meta {
  name: Revoke video wrong HTTP method GET
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/video/revoke
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test("Should return 405 Method Not Allowed", function() {
    expect(res.status).to.equal(405);
  });
}