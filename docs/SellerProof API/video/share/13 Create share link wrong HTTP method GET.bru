meta {
  name: Create share link wrong HTTP method GET
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/api/v1/video/share
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for GET method', function() {
    expect(res.status).to.equal(405);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Method Not Allowed');
  });
}