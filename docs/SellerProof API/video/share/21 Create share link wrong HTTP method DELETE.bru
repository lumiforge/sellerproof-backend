meta {
  name: Create share link wrong HTTP method DELETE
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/api/v1/video/share
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for DELETE method', function() {
    expect(res.status).to.equal(405);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Method Not Allowed');
  });
}