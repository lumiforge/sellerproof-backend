meta {
  name: Publish video without JWT token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/video/publish
  body: json
}

body:json {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 401 Unauthorized for request without JWT token', function() {
    expect(res.status).to.equal(401);
  });
}