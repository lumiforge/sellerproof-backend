meta {
  name: Update profile without JWT token
  type: http
  seq: 12
}

put {
  url: {{baseUrl}}/api/v1/auth/profile
  body: json
  auth: none
}

body:json {
  {
    "full_name": "Updated Test User"
  }
}

tests {
  test('Should return 401 Unauthorized for missing JWT token', function() {
    expect(res.status).to.equal(401);
  });
}