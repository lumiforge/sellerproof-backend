meta {
  name: Get invitations with expired JWT token
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/organization/invitations
  auth: bearer
}

auth:bearer {
  token: {{expiredToken}}
}

tests {
  test('Should return 401 Unauthorized for expired JWT token', function() {
    expect(res.status).to.equal(401);
  });
}