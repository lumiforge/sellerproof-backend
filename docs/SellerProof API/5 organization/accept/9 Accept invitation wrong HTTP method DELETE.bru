meta {
  name: Accept invitation wrong HTTP method DELETE
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for DELETE request', function() {
    expect(res.status).to.equal(405);
  });
}