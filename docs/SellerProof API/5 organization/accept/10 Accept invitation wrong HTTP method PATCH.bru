meta {
  name: Accept invitation wrong HTTP method PATCH
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PATCH request', function() {
    expect(res.status).to.equal(405);
  });
}