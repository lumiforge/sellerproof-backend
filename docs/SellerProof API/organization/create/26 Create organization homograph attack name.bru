meta {
  name: Create organization homograph attack name
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/api/v1/organization/create
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Теѕt Оrgаnіzаtіоn",  // Uses Cyrillic characters that look like Latin ones
    "description": "Test organization with homograph attack"
  }
}

tests {
  test('Should return 400 Bad Request when name field contains homograph attack', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.be.an('object');

    // Validate error response structure
    expect(res.body).to.have.property('error');
    expect(res.body).to.have.property('message');
    expect(res.body).to.have.property('code');

    // Validate error details
    expect(res.body.error).to.equal('Bad Request');
    expect(res.body.message).to.include('Unicode security');
    expect(res.body.code).to.equal(400);
  });
}