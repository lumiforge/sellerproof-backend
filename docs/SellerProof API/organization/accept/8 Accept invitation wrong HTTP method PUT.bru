meta {
  name: Accept invitation wrong HTTP method PUT
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PUT request', function() {
    expect(res.status).to.equal(405);
  });
}