meta {
  name: Delete invitation success with valid JWT
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/organization/invitations/{{validInvitationId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 200 OK and valid delete response for valid JWT and invitation ID', function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('object');

    // Validate all required fields exist
    expect(res.body).to.have.property('message');

    // Validate field types
    expect(res.body.message).to.be.a('string');

    // Validate field values are not empty
    expect(res.body.message).to.not.equal('');

    // Validate message content
    expect(res.body.message.toLowerCase()).to.include('deleted').or.include('removed');
  });
}