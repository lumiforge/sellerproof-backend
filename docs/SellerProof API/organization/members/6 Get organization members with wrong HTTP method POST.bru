meta {
  name: Get organization members with wrong HTTP method POST
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/organization/members?org_id={{orgId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for POST request', function() {
    expect(res.status).to.equal(405);
  });
}