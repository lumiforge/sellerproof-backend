meta {
  name: Get subscription with expired JWT token
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/organization/subscription
  body: none
  auth: bearer
}

auth:bearer {
  token: {{expiredAccessToken}}
}

tests {
  test('Should return 401 Unauthorized', function() {
    expect(res.status).to.equal(401);
  });
}