meta {
  name: Delete invitation wrong HTTP method PUT
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/v1/organization/invitations/{{validInvitationId}}
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PUT request', function() {
    expect(res.status).to.equal(405);
  });
}