meta {
  name: Delete invitation without JWT token
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/organization/invitations/{{validInvitationId}}
}

tests {
  test('Should return 401 Unauthorized when no JWT token is provided', function() {
    expect(res.status).to.equal(401);
  });
}