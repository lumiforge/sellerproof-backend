meta {
  name: Delete invitation wrong HTTP method GET
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/organization/invitations/{{validInvitationId}}
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for GET request', function() {
    expect(res.status).to.equal(405);
  });
}