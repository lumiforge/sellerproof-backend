meta {
  name: Create organization with invalid JWT token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/organization/create
  body: json
  auth: bearer
}

auth:bearer {
  token: invalid.token.here
}

body:json {
  {
    "name": "Test Organization",
    "description": "A test organization for testing purposes"
  }
}

tests {
  test('Should return 401 Unauthorized with invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}