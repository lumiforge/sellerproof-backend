meta {
  name: Create organization wrong HTTP method PUT
  type: http
  seq: 15
}

put {
  url: {{baseUrl}}/api/v1/organization/create
  body: json
}

tests {
  test('Should return 405 Method Not Allowed for PUT method', function() {
    expect(res.status).to.equal(405);
  });
}