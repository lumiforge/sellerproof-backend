meta {
  name: Create organization with malformed JWT token
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/organization/create
  body: json
  auth: bearer
}

auth:bearer {
  token: malformed.token
}

body:json {
  {
    "name": "Test Organization",
    "description": "A test organization for testing purposes"
  }
}

tests {
  test('Should return 401 Unauthorized with malformed JWT token', function() {
    expect(res.status).to.equal(401);
  });
}