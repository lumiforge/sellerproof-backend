meta {
  name: Get invitations wrong HTTP method PUT
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/api/v1/organization/invitations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "dummy": "data"
  }
}

tests {
  test('Should return 405 Method Not Allowed for PUT method', function() {
    expect(res.status).to.equal(405);
  });
}