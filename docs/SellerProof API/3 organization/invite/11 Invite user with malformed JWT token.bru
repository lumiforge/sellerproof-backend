meta {
  name: Invite user with malformed JWT token
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/organization/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.invalid.token
}

body:json {
  {
    "email": "test@example.com",
    "role": "user",
    "org_id": "{{testOrgId}}"
  }
}

tests {
  test('Should return 401 Unauthorized for request with malformed JWT token', function() {
    expect(res.status).to.equal(401);
  });
}