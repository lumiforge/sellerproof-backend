meta {
  name: Invite user with invalid JWT token
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/organization/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: invalid.jwt.token.here
}

body:json {
  {
    "email": "test@example.com",
    "role": "user",
    "org_id": "{{testOrgId}}"
  }
}

tests {
  test('Should return 401 Unauthorized for request with invalid JWT token', function() {
    expect(res.status).to.equal(401);
  
  });
}