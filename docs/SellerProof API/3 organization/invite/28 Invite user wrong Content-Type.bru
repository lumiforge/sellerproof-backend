meta {
  name: Invite user wrong Content-Type
  type: http
  seq: 28
}

post {
  url: {{baseUrl}}/api/v1/organization/invite
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:text {
  {
    "email": "test-extra-fields-111@example.com",
    "role": "user",
    "org_id": "{{testOrgId}}"
  }
}

tests {
  test('Should return 415 Unsupported Media Type for wrong Content-Type', function() {
    expect(res.status).to.equal(415);
  });
}