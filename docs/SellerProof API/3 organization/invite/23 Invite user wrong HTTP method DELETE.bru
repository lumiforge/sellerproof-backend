meta {
  name: Invite user wrong HTTP method DELETE
  type: http
  seq: 23
}

delete {
  url: {{baseUrl}}/api/v1/organization/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "email": "test@example.com",
    "role": "user",
    "org_id": "{{adminOrgId}}"
  }
}

tests {
  test('Should return 405 Method Not Allowed for DELETE method', function() {
    expect(res.status).to.equal(405);
  });
}