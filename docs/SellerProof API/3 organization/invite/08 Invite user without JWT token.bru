meta {
  name: Invite user without JWT token
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/organization/invite
  body: json
}

body:json {
  {
    "email": "test@example.com",
    "role": "user",
    "org_id": "{{testOrgId}}"
  }
}

tests {
  test('Should return 401 Unauthorized for request without JWT token', function() {
    expect(res.status).to.equal(401);
  });
}