meta {
  name: Accept invitation with expired JWT token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  body: json
  auth: bearer
}

auth:bearer {
  token: {{expiredAccessToken}}
}

body:json {
  {
    "invite_code": "SOME-CODE"
  }
}

tests {
  test('Should return 401 Unauthorized for request with expired JWT token', function() {
    expect(res.status).to.equal(401);
  });
}