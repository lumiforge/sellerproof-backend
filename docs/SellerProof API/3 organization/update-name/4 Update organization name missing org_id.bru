meta {
  name: Update organization name missing org_id
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/organization/name
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "name": "New Name"
  }
}

tests {
  test('Should return 400 Bad Request', function() {
    expect(res.status).to.equal(400);
    expect(res.body.message).to.include('org_id is required');
  });
}