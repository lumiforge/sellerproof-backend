meta {
  name: Update member status wrong HTTP method POST
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/organization/members/{{userId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "active"
  }
}

tests {
  test('Should return 405 Method Not Allowed for POST request', function() {
    expect(res.status).to.equal(405);
  });
}