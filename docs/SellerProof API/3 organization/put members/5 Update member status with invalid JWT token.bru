meta {
  name: Update member status with invalid JWT token
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/v1/organization/members/{{adminId}}/status
  body: json
}

headers {
  Authorization: Bearer invalid.jwt.token
}

body:json {
  {
    "status": "active"
  }
}

tests {
  test('Should return 401 for invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}