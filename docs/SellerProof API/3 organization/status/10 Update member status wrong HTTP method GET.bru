meta {
  name: Update member status wrong HTTP method GET
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/organization/members/{{adminId}}/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for GET request', function() {
    expect(res.status).to.equal(405);
  });
}