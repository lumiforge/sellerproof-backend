meta {
  name: Update member status wrong Content-Type
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/api/v1/organization/members/{{adminId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "status": "active"
  }
}

headers {
  Content-Type: application/x-www-form-urlencoded
}

tests {
  test('Should return 415 Unsupported Media Type for wrong Content-Type', function() {
    expect(res.status).to.equal(415);
  });
}