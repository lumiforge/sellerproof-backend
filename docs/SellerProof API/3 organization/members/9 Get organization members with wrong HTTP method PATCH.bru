meta {
  name: Get organization members with wrong HTTP method PATCH
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/v1/organization/members?org_id={{adminOrgId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed for PATCH request', function() {
    expect(res.status).to.equal(405);
  });
}