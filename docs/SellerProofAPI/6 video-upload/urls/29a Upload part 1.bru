meta {
  name: Upload part 1
  type: http
  seq: 29
}

put {
  url: {{adminOrgPartUrl1}}
  body: file
  auth: none
}

body:file {
  file: @file(6 video-upload/complete/test-video.mp4) @contentType(video/mp4)
}

tests {
  test("S3 should return 200 OK", function() {
    expect(res.status).to.equal(200);
    bru.setEnvVar("adminOrgEtag1", res.headers.etag.replace(/"/g, ''));
  });
}