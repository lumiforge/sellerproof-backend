meta {
  name: Accept invitation with invalid JWT token
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  body: json
  auth: bearer
}

auth:bearer {
  token: invalid.jwt.token.here
}

body:json {
  {
    "invite_code": "SOME-CODE"
  }
}

tests {
  test('Should return 401 Unauthorized for request with invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}