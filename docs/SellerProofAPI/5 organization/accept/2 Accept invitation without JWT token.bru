meta {
  name: Accept invitation without JWT token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/organization/invitations/accept
  body: json
}

body:json {
  {
    "invite_code": "SOME-CODE"
  }
}

tests {
  test('Should return 401 Unauthorized for request without JWT token', function() {
    expect(res.status).to.equal(401);
  });
}