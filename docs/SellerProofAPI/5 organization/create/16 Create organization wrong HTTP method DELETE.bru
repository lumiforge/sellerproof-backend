meta {
  name: Create organization wrong HTTP method DELETE
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/api/v1/organization/create
}

tests {
  test('Should return 405 Method Not Allowed for DELETE method', function() {
    expect(res.status).to.equal(405);
  });
}