meta {
  name: Create organization wrong HTTP method PATCH
  type: http
  seq: 17
}

patch {
  url: {{baseUrl}}/api/v1/organization/create
  body: json
}

tests {
  test('Should return 405 Method Not Allowed for PATCH method', function() {
    expect(res.status).to.equal(405);
  });
}