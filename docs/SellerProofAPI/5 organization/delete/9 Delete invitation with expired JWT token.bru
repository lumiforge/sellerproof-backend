meta {
  name: Delete invitation with expired JWT token
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/organization/invitations/{{validInvitationId}}
  auth: bearer
}

auth:bearer {
  token: {{expiredAccessToken}}
}

tests {
  test('Should return 401 Unauthorized when expired JWT token is provided', function() {
    expect(res.status).to.equal(401);
  });
}