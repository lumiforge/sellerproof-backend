meta {
  name: Get subscription wrong HTTP method PUT
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}/api/v1/organization/subscription
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 405 Method Not Allowed', function() {
    expect(res.status).to.equal(405);
  });
}