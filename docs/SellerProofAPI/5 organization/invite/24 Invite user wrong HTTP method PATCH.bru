meta {
  name: Invite user wrong HTTP method PATCH
  type: http
  seq: 24
}

patch {
  url: {{baseUrl}}/api/v1/organization/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "email": "test@example.com",
    "role": "user",
    "org_id": "{{adminOrgId}}"
  }
}

tests {
  test('Should return 405 Method Not Allowed for PATCH method', function() {
    expect(res.status).to.equal(405);
  });
}