meta {
  name: Switch organization wrong HTTP method GET
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/auth/switch-organization
  auth: inherit
}

tests {
  test('Should return 405 Method Not Allowed for GET request to switch organization endpoint', function() {
    expect(res.status).to.equal(405);
  });
}