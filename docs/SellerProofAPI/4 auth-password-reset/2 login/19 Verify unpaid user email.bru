meta {
  name: Verify unpaid user email
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/api/v1/auth/verify-email
  body: json
  auth: none
}

body:json {
  {
    "email": "{{unpaidUserEmail}}",
    "code": "{{unpaidUserVerificationCode}}"
  }
}

tests {
  test("Should return 200 for successful verification", function() {
    expect(res.status).to.equal(200);
  });
}