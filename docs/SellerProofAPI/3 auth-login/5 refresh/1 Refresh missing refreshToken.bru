meta {
  name: Refresh missing refresh_token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
  }
}

tests {
  test('Should return 400 Bad Request for missing refresh_token field', function() {
    expect(res.status).to.equal(400);
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('Bad Request');
  });
}