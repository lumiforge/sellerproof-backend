meta {
  name: Refresh wrong HTTP method DELETE
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "valid_refresh_token_string"
  }
}

tests {
  test('Should return 405 Method Not Allowed for DELETE request', function() {
    expect(res.status).to.equal(405);
  });
}