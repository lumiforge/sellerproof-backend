meta {
  name: Get profile with empty JWT token
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: ""
}

tests {
  test('Should return 401 Unauthorized for empty JWT token', function() {
    expect(res.status).to.equal(401);
  });
}