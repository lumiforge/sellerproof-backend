meta {
  name: Verify restored video
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/video?video_id={{deletedVideoId}}
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 200 OK and active status', function() {
    expect(res.status).to.equal(200);
    expect(res.body.upload_status).to.equal('completed');
  });
}