meta {
  name: Verify deleted video (Trash)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/video?video_id={{deletedVideoId}}
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test('Should return 404 for deleted video (moved to trash)', function() {
    expect(res.status).to.equal(404);
  });
}