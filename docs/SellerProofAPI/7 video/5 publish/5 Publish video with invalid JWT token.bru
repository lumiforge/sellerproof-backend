meta {
  name: Publish video with invalid JWT token
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/video/publish
  body: json
  auth: bearer
}

auth:bearer {
  token: invalid.jwt.token
}

body:json {
  {
    "video_id": "{{testVideoId}}"
  }
}

tests {
  test('Should return 401 Unauthorized for request with invalid JWT token', function() {
    expect(res.status).to.equal(401);
  });
}