meta {
  name: Publish video large file (should fail)
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/api/v1/video/publish
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

body:json {
  {
    "video_id": "{{largeVideoId}}"
  }
}

tests {
  test('Should return 400 or 404 because video was not created', function() {
    // Since initiation failed, the video_id variable is likely invalid or empty
    expect(res.status).to.be.oneOf([400, 404]);
  });
}