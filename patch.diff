diff --git a/internal/ydb/client.go b/internal/ydb/client.go
index c06a394..e5b8a12 100644
--- a/internal/ydb/client.go
+++ b/internal/ydb/client.go
@@ -1648,7 +1648,7 @@ func (c *YDBClient) GetStorageUsage(ctx context.Context, ownerID string) (int64,
 	`
 
 	var usage int64
-	var count int64
+	var count uint64
 
 	err := c.driver.Table().Do(ctx, func(ctx context.Context, session table.Session) error {
 		_, res, err := session.Execute(ctx, table.DefaultTxControl(), query,
@@ -1676,5 +1676,5 @@ func (c *YDBClient) GetStorageUsage(ctx context.Context, ownerID string) (int64,
 	if err != nil {
 		return 0, 0, err
 	}
-	return usage, count, nil
+	return usage, int64(count), nil
 }
 
 // PublishVideoTx выполняет публикацию видео в одной транзакции: