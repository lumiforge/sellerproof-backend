diff --git a/internal/ydb/client.go b/internal/ydb/client.go
index f6b1e3a..9a4e5c2 100644
--- a/internal/ydb/client.go
+++ b/internal/ydb/client.go
@@ -3,6 +3,7 @@ package ydb
 import (
 	"context"
+	"database/sql"
 	"fmt"
 	"log"
 
@@ -477,6 +478,11 @@ func (c *YDBClient) GetUserByID(ctx context.Context, userID string) (*User, erro
 	var found bool
 
 	err := c.driver.Table().Do(ctx, func(ctx context.Context, session table.Session) error {
+		var (
+			fullName, verificationCode sql.NullString
+			verificationExpiresAt      sql.NullTime
+		)
+
 		_, res, err := session.Execute(ctx, table.DefaultTxControl(), query,
 			table.NewQueryParameters(
 				table.ValueParam("$user_id", types.TextValue(userID)),
@@ -492,16 +498,25 @@ func (c *YDBClient) GetUserByID(ctx context.Context, userID string) (*User, erro
 				named.Required("user_id", &user.UserID),
 				named.Required("email", &user.Email),
 				named.Required("password_hash", &user.PasswordHash),
-				named.Optional("full_name", &user.FullName),
+				named.Optional("full_name", &fullName),
 				named.Required("email_verified", &user.EmailVerified),
-				named.Optional("verification_code", &user.VerificationCode),
-				named.Optional("verification_expires_at", &user.VerificationExpiresAt),
+				named.Optional("verification_code", &verificationCode),
+				named.Optional("verification_expires_at", &verificationExpiresAt),
 				named.Required("created_at", &user.CreatedAt),
 				named.Required("updated_at", &user.UpdatedAt),
 				named.Required("is_active", &user.IsActive),
 			)
 			if err != nil {
 				return fmt.Errorf("scan failed: %w", err)
 			}
+			if fullName.Valid {
+				user.FullName = &fullName.String
+			}
+			if verificationCode.Valid {
+				user.VerificationCode = &verificationCode.String
+			}
+			if verificationExpiresAt.Valid {
+				user.VerificationExpiresAt = &verificationExpiresAt.Time
+			}
 		}
 		return res.Err()
 	})
@@ -532,6 +547,11 @@ func (c *YDBClient) GetUserByEmail(ctx context.Context, email string) (*User, er
 	var found bool
 
 	err := c.driver.Table().Do(ctx, func(ctx context.Context, session table.Session) error {
+		var (
+			fullName, verificationCode sql.NullString
+			verificationExpiresAt      sql.NullTime
+		)
+
 		_, res, err := session.Execute(ctx, table.DefaultTxControl(), query,
 			table.NewQueryParameters(
 				table.ValueParam("$email", types.TextValue(email)),
@@ -547,17 +567,26 @@ func (c *YDBClient) GetUserByEmail(ctx context.Context, email string) (*User, er
 				named.Required("user_id", &user.UserID),
 				named.Required("email", &user.Email),
 				named.Required("password_hash", &user.PasswordHash),
-				named.Optional("full_name", &user.FullName),
+				named.Optional("full_name", &fullName),
 				named.Required("email_verified", &user.EmailVerified),
-				named.Optional("verification_code", &user.VerificationCode),
-				named.Optional("verification_expires_at", &user.VerificationExpiresAt),
+				named.Optional("verification_code", &verificationCode),
+				named.Optional("verification_expires_at", &verificationExpiresAt),
 				named.OptionalWithDefault("created_at", &user.CreatedAt),
 				named.OptionalWithDefault("updated_at", &user.UpdatedAt),
 				named.Required("is_active", &user.IsActive),
 			)
 
 			if err != nil {
 				return fmt.Errorf("scan failed: %w", err)
 			}
-
+			if fullName.Valid {
+				user.FullName = &fullName.String
+			}
+			if verificationCode.Valid {
+				user.VerificationCode = &verificationCode.String
+			}
+			if verificationExpiresAt.Valid {
+				user.VerificationExpiresAt = &verificationExpiresAt.Time
+			}
 		}
 		return res.Err()
 	})