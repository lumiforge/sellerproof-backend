diff --git a/internal/http/handlers.go b/internal/http/handlers.go
index 2872345..9876543 100644
--- a/internal/http/handlers.go
+++ b/internal/http/handlers.go
@@ -1134,6 +1134,7 @@ func (s *Server) GetVideo(w http.ResponseWriter, r *http.Request) {
 	videoResp := &models.Video{
 		VideoID:         resp.VideoID,
 		Title:           resp.Title,
+		AuthorName:      resp.AuthorName,
 		FileName:        resp.FileName,
 		FileSizeBytes:   resp.FileSizeBytes,
 		DurationSeconds: resp.DurationSeconds,
@@ -1202,6 +1203,7 @@ func (s *Server) SearchVideos(w http.ResponseWriter, r *http.Request) {
 		videos[i] = &models.Video{
 			VideoID:         v.VideoID,
 			Title:           v.Title,
+			AuthorName:      v.AuthorName,
 			FileName:        v.FileName,
 			FileSizeBytes:   v.FileSizeBytes,
 			DurationSeconds: v.DurationSeconds,
diff --git a/internal/http/handlers_test.go b/internal/http/handlers_test.go
index 1234567..8901234 100644
--- a/internal/http/handlers_test.go
+++ b/internal/http/handlers_test.go
@@ -559,6 +559,7 @@ func TestHandler_GetVideo_Success(t *testing.T) {
 		UploadStatus:  "completed",
 		PublishStatus: "published",
 	}, nil)
+	mockDB.On("GetUserByID", mock.Anything, "user-1").Return(&ydb.User{FullName: "Test User"}, nil)
 
 	req := httptest.NewRequest("GET", "/api/v1/video?video_id="+videoID, nil)
 	req.Header.Set("Authorization", "Bearer "+token)
@@ -589,6 +590,7 @@ func TestHandler_SearchVideos_Success(t *testing.T) {
 		},
 	}
 	mockDB.On("SearchVideos", mock.Anything, "org-1", "user-1", "test", 10, 0).Return(videos, int64(1), nil)
+	mockDB.On("GetUserByID", mock.Anything, "user-1").Return(&ydb.User{FullName: "Test User"}, nil)
 
 	req := httptest.NewRequest("GET", "/api/v1/video/search?query=test", nil)
 	req.Header.Set("Authorization", "Bearer "+token)
diff --git a/internal/models/video.go b/internal/models/video.go
index abcdef0..1234567 100644
--- a/internal/models/video.go
+++ b/internal/models/video.go
@@ -69,6 +69,7 @@ type CompleteMultipartUploadResponse struct {
 type Video struct {
 	VideoID         string `json:"video_id"`
 	Title           string `json:"title"`
+	AuthorName      string `json:"author_name"`
 	FileName        string `json:"file_name"`
 	FileSizeBytes   int64  `json:"file_size_bytes"`
 	DurationSeconds int32  `json:"duration_seconds"`
diff --git a/internal/video/service.go b/internal/video/service.go
index 5678901..2345678 100644
--- a/internal/video/service.go
+++ b/internal/video/service.go
@@ -49,6 +49,7 @@ type CompletedPart struct {
 type VideoInfo struct {
 	VideoID         string `json:"video_id"`
 	Title           string `json:"title"`
+	AuthorName      string `json:"author_name"`
 	FileName        string `json:"file_name"`
 	FileSizeBytes   int64  `json:"file_size_bytes"`
 	DurationSeconds int32  `json:"duration_seconds"`
@@ -467,6 +468,12 @@ func (s *Service) GetVideoDirect(ctx context.Context, userID, orgID, role, video
 		return nil, app_errors.ErrAccessDenied
 	}
 
+	user, err := s.db.GetUserByID(ctx, video.UploadedBy)
+	authorName := ""
+	if err == nil {
+		authorName = user.FullName
+	}
+
 	var uploadedAt int64
 	if video.UploadedAt != nil {
 		uploadedAt = video.UploadedAt.Unix()
@@ -479,6 +486,7 @@ func (s *Service) GetVideoDirect(ctx context.Context, userID, orgID, role, video
 	return &VideoInfo{
 		VideoID:         video.VideoID,
 		Title:           video.Title,
+		AuthorName:      authorName,
 		FileName:        fileName,
 		FileSizeBytes:   fileSizeBytes,
 		DurationSeconds: durationSeconds,
@@ -616,6 +624,7 @@ func (s *Service) SearchVideosDirect(ctx context.Context, userID, orgID, role, q
 	}
 
 	videoInfos := make([]*VideoInfo, len(videos))
+	userNames := make(map[string]string)
 	for i, v := range videos {
 		var uploadedAt int64
 		if v.UploadedAt != nil {
@@ -626,9 +635,20 @@ func (s *Service) SearchVideosDirect(ctx context.Context, userID, orgID, role, q
 		durationSeconds := v.DurationSeconds
 		uploadStatus := v.UploadStatus
 		publishStatus := v.PublishStatus
+
+		authorName := ""
+		if name, ok := userNames[v.UploadedBy]; ok {
+			authorName = name
+		} else {
+			if user, err := s.db.GetUserByID(ctx, v.UploadedBy); err == nil {
+				authorName = user.FullName
+			}
+			userNames[v.UploadedBy] = authorName
+		}
+
 		videoInfos[i] = &VideoInfo{
 			VideoID:         v.VideoID,
 			Title:           v.Title,
+			AuthorName:      authorName,
 			FileName:        fileName,
 			FileSizeBytes:   fileSizeBytes,
 			DurationSeconds: durationSeconds,
diff --git a/internal/video/service_test.go b/internal/video/service_test.go
index 3456789..0123456 100644
--- a/internal/video/service_test.go
+++ b/internal/video/service_test.go
@@ -370,6 +370,7 @@ func TestService_GetVideoDirect_Success(t *testing.T) {
 		UploadStatus:  "completed",
 		PublishStatus: "published",
 	}, nil)
+	mockDB.On("GetUserByID", ctx, userID).Return(&ydb.User{FullName: "Test User"}, nil)
 
 	info, err := service.GetVideoDirect(ctx, userID, orgID, "user", videoID)