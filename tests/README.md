# Тестовый клиент для SellerProof Backend

Этот тестовый клиент предназначен для проверки работоспособности REST API сервиса SellerProof Backend, развернутого на Yandex Cloud.

## ⚠️ Важная информация о URL

**Тестовый клиент использует переменную окружения `SERVICE_URL` для URL сервиса!**

Для правильной работы тестового клиента установите переменную окружения `SERVICE_URL` с вашим реальным URL API Gateway в Yandex Cloud:

```bash
# Способ 1: Через переменную окружения SERVICE_URL (рекомендуется)
export SERVICE_URL="https://ваш-реальный-url.apigw.yandexcloud.net"
make test

# Способ 2: Через Makefile
make build SERVICE_URL="https://ваш-реальный-url.apigw.yandexcloud.net"

# Способ 3: Через скрипт
./run_tests.sh -u "https://ваш-реальный-url.apigw.yandexcloud.net"
```

## 🔍 Возможные ошибки и их решения

### Ошибка: "API Gateway not found by host name"
**Причина:** Указанный URL API Gateway не существует или недоступен.
**Решение:** Укажите правильный URL вашего API Gateway в Yandex Cloud.

### Ошибка: "connection error"
**Причина:** Проблемы с сетевым подключением или TLS.
**Решение:** Проверьте доступность сервиса и настройки файрвола.

### Ошибка: "request failed with status 404"
**Причина:** Неправильный путь API или эндпоинт не существует.
**Решение:** Убедитесь, что используете правильные REST API эндпоинты.

## 🚀 Быстрый старт

### 1. Сборка и запуск тестов

```bash
# Сборка и запуск тестов
make test

# Или просто запуск, если бинарник уже собран
make run

# Или запуск с помощью скрипта
./run_tests.sh
```

### 2. Запуск с отладочными логами

```bash
make debug

# Или с помощью скрипта
./run_tests.sh -d
```

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `make build` | Сборка тестового клиента |
| `make test` | Сборка и запуск тестов |
| `make run` | Запуск тестов без сборки |
| `make clean` | Очистка |
| `make debug` | Запуск с отладочными логами |
| `make deps` | Проверка зависимостей |
| `make fmt` | Форматирование кода |
| `make vet` | Проверка кода |
| `make help` | Показать справку |
| `./run_tests.sh` | Запуск с дополнительными опциями |

## 🔧 Конфигурация

URL сервиса указан в Makefile и передается в бинарник при сборке:

```makefile
SERVICE_URL = https://xxxj.4b4k4pg5.apigw.yandexcloud.net
```

Вы можете изменить его несколькими способами:

### 1. Через Makefile (рекомендуется):
```bash
make build SERVICE_URL="https://your-service-url.com"
```

### 2. Через переменные окружения (имеет приоритет над Makefile):
```bash
export SERVICE_URL="https://your-service-url.com"
./test-client
```

### 3. Через скрипт run_tests.sh:
```bash
./run_tests.sh -u https://your-service-url.com
```

## 📝 Тестируемые функции

### Аутентификация (REST API)
- ✅ Регистрация нового пользователя (`POST /api/v1/auth/register`)
- ✅ Вход пользователя (`POST /api/v1/auth/login`)
- ✅ Получение профиля (`GET /api/v1/auth/profile`)
- ✅ Обновление профиля (`PUT /api/v1/auth/profile`)
- ⚠️ Обновление токена (требует refresh токен)
- ⚠️ Выход (требует refresh токен)

### Видео (REST API)
- ✅ Инициализация загрузки видео (`POST /api/v1/video/upload/initiate`)
- ✅ Получение URL для загрузки частей (`POST /api/v1/video/upload/urls`)
- ✅ Завершение загрузки видео (`POST /api/v1/video/upload/complete`)
- ✅ Получение информации о видео (`GET /api/v1/video`)
- ✅ Поиск видео (`GET /api/v1/video/search`)
- ✅ Создание публичной ссылки (`POST /api/v1/video/share`)
- ✅ Получение публичного видео (`GET /api/v1/video/public`)
- ✅ Отзыв публичной ссылки (`POST /api/v1/video/share/revoke`)

## 📊 Пример вывода

```
🚀 Запуск тестов для SellerProof Backend
📡 Подключение к сервису: https://xxxj.4b4k4pg5.apigw.yandexcloud.net
🔗 Адрес для REST API: https://xxxj.4b4k4pg5.apigw.yandexcloud.net
⏱️  Таймаут: 30 секунд
🎯 Режим: все тесты

🔐 Запуск тестов аутентификации...
📝 Тестирование регистрации пользователя...
[❌ ОШИБКА] Регистрация
   Ошибка: request failed with status 404: Not Found

...

✅ Все тесты завершены!
```

## ⚠️ Важные замечания

1. **URL сервиса**: Убедитесь, что URL сервиса `https://xxxj.4b4k4pg5.apigw.yandexcloud.net` корректен и сервис доступен
2. **Сетевое подключение**: Убедитесь, что у вас есть доступ к интернету и нет блокировок
3. **HTTPS**: Клиент использует стандартные HTTP запросы для подключения к HTTPS
4. **Тестовые данные**: Некоторые тесты используют тестовые ID и данные, которые могут не существовать в системе
5. **REST API**: Убедитесь, что API Gateway настроен для работы с REST API эндпоинтами

## 🛠️ Дополнительные опции скрипта

Скрипт `run_tests.sh` поддерживает следующие опции:

```bash
./run_tests.sh [ОПЦИИ]

ОПЦИИ:
  -h, --help              Показать эту справку
  -u, --url URL           URL сервиса (по умолчанию: https://xxxj.4b4k4pg5.apigw.yandexcloud.net)
  -d, --debug             Запуск с отладочными логами
  -v, --verbose           Подробный вывод
  -b, --build             Только сборка, без запуска
  -c, --clean             Очистка перед сборкой
  -t, --timeout SECONDS   Таймаут для тестов (по умолчанию: 30)
  --auth-only             Запустить только тесты аутентификации
  --video-only            Запустить только тесты видео
  --no-color              Отключить цветной вывод
```

### Примеры использования

```bash
# Запуск всех тестов
./run_tests.sh

# Запуск с отладочными логами
./run_tests.sh -d

# Запуск с другим URL
./run_tests.sh -u https://example.com

# Только тесты аутентификации
./run_tests.sh --auth-only

# Только тесты видео
./run_tests.sh --video-only

# С таймаутом 60 секунд
./run_tests.sh -t 60
```

## 🐛 Отладка

Для детальной отладки HTTP-запросов можно использовать переменную окружения `GODEBUG`:

```bash
GODEBUG=http2debug=1 ./test-client
```

Или использовать готовую команду:

```bash
make debug
```

## ⚠️ Ограничения

1. Некоторые тесты требуют наличия реальных данных в системе (например, существующего пользователя для входа)
2. Тесты обновления токена и выхода требуют сохранения refresh токена из ответа входа
3. Тесты видео используют тестовые ID, которые могут не существовать в системе
4. REST API требует правильной настройки CORS для работы с веб-клиентами

## 🔧 Настройка

Для изменения параметров тестов отредактируйте соответствующие секции в файле `cmd/test-client/main.go`:

- Изменить URL сервиса: переменная `serviceURL`
- Изменить тестовые данные: функции `test*()`
- Добавить новые тесты: добавить новые методы в структуру `TestClient`

## 📝 Требования

- Go 1.24.2 или выше
- Доступ к интернету для подключения к сервису
- Права на выполнение в директории `tests/`
- Bash для запуска скрипта `run_tests.sh`
- REST API совместимый сервис

## 🤝 Вклад

Если вы хотите добавить новые тесты или улучшить существующие, пожалуйста:

1. Создайте новую ветку
2. Добавьте или измените тесты
3. Убедитесь, что код проходит проверку (`make fmt` и `make vet`)
4. Отправьте pull request

## 📁 Структура проекта

```
tests/
├── cmd/
│   └── test-client/
│       └── main.go          # Основной файл тестового клиента
├── go.mod                  # Модуль Go
├── go.sum                  # Хэши зависимостей
├── Makefile                # Makefile для удобной сборки
├── README.md               # Этот файл
├── run_tests.sh            # Скрипт для запуска с опциями
└── test-client             # Скомпилированный бинарник