

## 👤 **Первый пользователь (admin)**

**Роль:** Владелец организации A

**Зачем нужен:**
- Основной пользователь для всех позитивных сценариев
- Создает организацию A при регистрации
- Загружает видео
- Управляет организацией (приглашает пользователей)
- Проверяет все основные функции системы

**Тестирует:**
- ✅ Регистрацию и верификацию
- ✅ Загрузку видео (initiate, urls, complete)
- ✅ Публикацию видео (share)
- ✅ Управление организацией (invite, members)
- ✅ Права admin (может всё)

***

## 👤 **Второй пользователь (admin)**

**Роль:** Владелец организации B (другой, независимой)

**Зачем нужен:**
- Проверяет **изоляцию между организациями**
- Создает свою собственную организацию B при регистрации
- НЕ должен иметь доступ к ресурсам организации A

**Тестирует:**
- ✅ **Критичная проверка безопасности:** пользователь из организации B не может скачать видео организации A

**Пример теста (Фаза 4):**
```javascript
// Второй пользователь пытается скачать видео первого
GET /api/v1/video/download?video_id={{testVideoId}}
Authorization: Bearer {{secondUserAccessToken}}

// Ожидается: 403 Forbidden
// Потому что testVideoId принадлежит организации A,
// а secondUser в организации B
```

**Без второго пользователя:** Нельзя проверить, что система правильно изолирует данные между организациями!

***

## 👤 **Третий пользователь (user)**

**Роль:** Приглашенный участник организации A с ограниченными правами

**Зачем нужен:**
- Проверяет **RBAC (систему ролей) внутри одной организации**
- Присоединяется к организации A по приглашению
- Имеет роль `user` (не `admin`, не `manager`)

**Тестирует:**
- ✅ Может читать/скачивать видео своей организации
- ❌ Не может загружать видео (требуется manager/admin)
- ❌ Не может приглашать пользователей (требуется manager/admin)

**Пример тестов (Фаза 6):**
```javascript
// Третий пользователь скачивает видео своей организации
GET /api/v1/video/download?video_id={{testVideoId}}
Authorization: Bearer {{thirdUserAccessToken}}
// Ожидается: 200 OK (в одной организации с первым)

// Третий пользователь пытается загрузить видео
POST /api/v1/video/upload/initiate
Authorization: Bearer {{thirdUserAccessToken}}
// Ожидается: 403 Forbidden (role=user не может)
```

**Без третьего пользователя:** Нельзя проверить, что роли работают правильно!

***

## 📊 Итоговая таблица:

| Пользователь | Организация | Роль | Что тестирует |
|--------------|-------------|------|---------------|
| **Первый** | A | `admin` | Основной функционал, права admin |
| **Второй** | B | `admin` | Изоляция между организациями |
| **Третий** | A | `user` | RBAC внутри организации, ограниченные права |

***

## 🎯 Критичные проверки безопасности:

### Без второго пользователя:
❌ Не проверяется: "Может ли пользователь из org B украсть данные org A?"

### Без третьего пользователя:
❌ Не проверяется: "Работают ли роли? Может ли user делать то, что не должен?"

***

## Визуализация:

```
┌─────────────────────────────────────┐
│      Организация A                  │
│  ┌──────────────┐  ┌──────────────┐│
│  │ Первый user  │  │ Третий user  ││
│  │ role: admin  │  │ role: user   ││
│  └──────────────┘  └──────────────┘│
│         │                   │       │
│         └───────┬───────────┘       │
│                 │                   │
│          ┌──────▼──────┐            │
│          │  Видео #1   │            │
│          └─────────────┘            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│      Организация B                  │
│  ┌──────────────┐                   │
│  │ Второй user  │                   │
│  │ role: admin  │                   │
│  └──────────────┘                   │
│         │                           │
│         │ ❌ Не должен видеть       │
│         │    видео из org A         │
└─────────────────────────────────────┘
```

***

## Вывод:

- **Первый** = главный актор, проверяет функционал
- **Второй** = проверяет безопасность между организациями (multi-tenancy)
- **Третий** = проверяет систему ролей внутри организации (RBAC)

Все три пользователя необходимы для полноценного тестирования системы! 🔒


1. health
2. Auth -> Register
 - Регистрируются три пользователя: admin, manager, simple user
3. Auth -> Копируем verification_code из user на облаке (для 3х пользователей)
4. Auth -> Login
 - Авторизуются пользователи: admin, manager, simple user и сохраняются 
   - AccessToken
   - RefreshToken
   - OrgId
   - Id
5. Organization -> Create
 - Создается организация новая организация для админа менеджера и простого пользователя и сохраняется их ID
6. Organization -> Invite
 - Приглашаем менеджера и простого пользователя в основную организацию админа
 - Приглашаем менеджера в новую организацию простого пользователя
 - Пытаемся с ошибкой пригласить админа в новую организацию простого пользователя админом
7. Organization -> Accept 
 - Принимаем приглашение в организацию админа простого пользователя
 - Принимаем приглашение в организацию админа менеджера
 - Принимаем приглашение в организацию простого пользователя менеджера
8. Organization -> Status
- Admin блокирует Manager → Проверка блокировки через `switch-organization` → Разблокировка → Проверка разблокировки
- Admin блокирует Simple User → Проверка блокировки → Manager блокирует Simple User → Разблокировка → Проверка разблокировки
9.  Organization -> Members 
10. Refresh
11. Logout
12. Profile
13. Video -> Upload -> Initiate
14. Video -> Upload -> URLs
15. Сслыки копируем URLs -> Get upload URLs success with valid JWT и для второго пользователя тоже
16. Оба раза вставляем URLs в run.sh file -> запускаем run.sh (**5 РАЗ**)
17. Из терминала, запускаем run.sh -> копируем etag в docs/SellerProof API/video/upload/complete/2 Complete upload success with valid JWT.bru
18. Complete run
19. Video get: Проверить, что видео действительно существует, статус сменился на completed, и метаданные (размер, имя) корректны.
20. Video search: Проверить, что видео индексируется и его можно найти в списке (поиск по имени или просто листинг).
21. Video download: Проверить (ПРОВЕРИТЬ ССЫЛКУ ИЗ 1), что владелец может получить временную ссылку (presigned URL) на скачивание приватного файла.
22. Video publish: Опубликовать видео (сделать его доступным публично). Это создаст public_token.
23. Video public: Проверить доступ к видео по публичному токену (без авторизации), который был получен на шаге publish.